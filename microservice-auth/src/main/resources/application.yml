server:
  port: 8090

spring:
  application:
    security:
      jwt:
        secret-key:

        expiration:
        refresh-token:
          expiration:
    name: msvc-auth
  config:
    import: optional:configserver:http://localhost:8888 # path del config server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/msvc_auth_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka